import styled from "styled-components";

const Header = styled.header`
  background: rgba(15, 22, 36, 0.9);
  position: fixed;
  padding: 20px 0;
  top: 0;
  left: 0;
  right: 0;
  z-index: 999999;
`;


export const S = {
   Header,
}